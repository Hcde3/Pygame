import turtle
scr = turtle.Screen()
turtle.pendown()
turtle.speed(100)
for y in range(12):
    for x in range(8):
        turtle.begin_fill()
        turtle.forward(100)
        turtle.left(45)
    turtle.right(30)
turtle.end_fill()
turtle.exitonclick()
