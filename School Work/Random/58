import random
point = 0
for x in range(5):
    num1 = random.randint(1,100)
    num2 = random.randint(1,100)
    question = "What is "+str(num1)+" + "+str(num2)+"?\n"
    if int(input(question)) == num1 + num2:
        point += 1
print("You got",point,"questions correct.")
